Search for movies
---
tags:
  - Movies
parameters:
  - in: query
    type: string
    name: search
    description: movie title to search for
responses:
  200:
    description: A list of movies
    schema:
      $ref: '#/definitions/Movies'
